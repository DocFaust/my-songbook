spring.security.oauth2.resourceserver.jwt.issuer-uri=https://test.invalid/realms/test
FRONTEND_ORIGIN=http://localhost:5173
# Test classpath shadows main application.properties; keep ddl-auto here so Hibernate
# validates the Flyway schema in Testcontainers tests.
spring.jpa.hibernate.ddl-auto=validate
